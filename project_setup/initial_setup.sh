# install all dependencies
npm install

# install nodemon as global function so we can access it just by typing 'nodemon server.js'
npm install -g nodemon

# configure git (e.g .gitignore, etc.)
sh project_setup/git_setup.sh